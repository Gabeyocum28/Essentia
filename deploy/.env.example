# Copy to /home/ubuntu/stacks/essentia/deploy/.env on the VM and fill in.
# Never commit the real file.
MONGODB_URI=
MONGODB_DB=essentia
PUBLIC_BASE_URL=https://essentia.gabeyocum.com/api
# The /viz endpoints keep a dense n×n similarity matrix; 10k is the
# documented ceiling.
CORPUS_CAP=10000
CRAWL_INTERVAL_S=60
